<%# add navigation links to this file %>
<% if user_signed_in? %>
	<li class="dropdown">
    	<%= link_to(raw("Mi cuenta <span class='caret'></span>"),"#",{ :id => 'profile_menu', :class => 'dropdown-toggle', :data => {:toggle => "dropdown"}})  %>
		<ul class="dropdown-menu" aria-labelledby="profile_menu">
			<li> 
				<%= link_to "Modificar mi cuenta", edit_user_registration_path,{:class => 'dropdown-toggle'}  %> 
			</li>
			<li>
				<%= link_to "Salir", destroy_user_session_path, :method => :delete %>
			</li>
		</ul>
	</li>

  
<% else %>
  <li><%= link_to 'Ingresar', new_user_session_path %></li>
  <li><%link_to 'Registrarse', new_user_registration_path %></li>
<% end %>
<% if user_signed_in? %>
  <li><% link_to 'Users', users_path %></li>
<% end %>
